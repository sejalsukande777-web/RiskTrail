# Python
__pycache__/
*.pyc
venv/
.env

# Node
node_modules/
dist/
.vite/

# Models / artifacts
# NOTE: ml/model.joblib is intentionally committed (not ignored) so the
# backend/demo don't require retraining locally. If the model grows large
# or is retrained often, revisit this.
*.pkl

# OS
.DS_Store

data/creditcard.csv