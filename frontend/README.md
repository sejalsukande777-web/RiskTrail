# RiskTrail Frontend

Simple React dashboard for the RiskTrail AI Fraud Investigation Copilot.

## Setup and Run

1. Open terminal inside the `frontend/` folder.
2. Install dependencies:
   ```bash
   npm install